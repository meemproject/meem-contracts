export enum Chain {
	Ethereum = 0,
	Polygon = 1,
	Cardano = 2
}

export enum PermissionType {
	Copy = 0,
	Remix = 1,
	Read = 2
}

export enum Permission {
	Owner = 0,
	Anyone = 1,
	Addresses = 2,
	Holders = 3
}

export enum PropertyType {
	Meem = 1,
	Child = 2
}
